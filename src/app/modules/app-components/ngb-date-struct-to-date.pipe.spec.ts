import { NgbDateStructToDatePipe } from './ngb-date-struct-to-date.pipe';

describe('NgbDateStructToDatePipe', () => {
  it('create an instance', () => {
    const pipe = new NgbDateStructToDatePipe();
    expect(pipe).toBeTruthy();
  });
});
