import { DateToNgbDateStructPipe } from './date-to-ngb-date-struct.pipe';

describe('DateToNgbDateStructPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToNgbDateStructPipe();
    expect(pipe).toBeTruthy();
  });
});
